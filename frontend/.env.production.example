# =============================================================================
# DR. CHAFFEE AI - FRONTEND CONFIGURATION (PRODUCTION)
# =============================================================================
# For production deployment on Vercel/Netlify/Render
# Set these as environment variables in your hosting platform

# =============================================================================
# BACKEND API URL (CRITICAL)
# =============================================================================
# Your production backend URL (Hetzner + Coolify deployment)
# Example: https://api.askdrchaffee.com
NEXT_PUBLIC_BACKEND_URL=https://your-backend-domain.com

# =============================================================================
# DATABASE (for Next.js API routes)
# =============================================================================
# PostgreSQL connection string (managed PostgreSQL on Hetzner or external)
# Example: postgresql://user:pass@postgres.internal:5432/askdrchaffee
DATABASE_URL=postgresql://user:password@host:5432/askdrchaffee

# =============================================================================
# OPENAI (for answer generation via Next.js API routes)
# =============================================================================
# OpenAI API key for answer generation
OPENAI_API_KEY=sk-proj-your_production_key_here

# Model: gpt-4o-mini (recommended for production)
SUMMARIZER_MODEL=gpt-4o-mini

# =============================================================================
# BACKEND API INTEGRATION (for Next.js API routes)
# =============================================================================
# Backend API URL for server-side calls (same as NEXT_PUBLIC_BACKEND_URL)
BACKEND_API_URL=https://your-backend-domain.com

# =============================================================================
# ANSWER SETTINGS
# =============================================================================
# Number of context chunks to retrieve for answer generation
ANSWER_TOPK=100

# Default answer style: 'concise' or 'detailed'
ANSWER_STYLE_DEFAULT=concise

# Answer cache TTL in hours (14 days = 336 hours)
ANSWER_CACHE_TTL_HOURS=336

# =============================================================================
# AUTHENTICATION (optional)
# =============================================================================
# Simple password for admin features (leave empty to disable)
APP_PASSWORD=your_secure_production_password

# =============================================================================
# RATE LIMITING (optional)
# =============================================================================
# Rate limits per minute per IP
RATE_LIMIT_CONCISE=10
RATE_LIMIT_DETAILED=3

# =============================================================================
# MONITORING (optional)
# =============================================================================
# Enable answer caching
ANSWER_ENABLED=true

# Node environment
NODE_ENV=production
