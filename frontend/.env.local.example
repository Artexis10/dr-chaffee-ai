# =============================================================================
# DR. CHAFFEE AI - FRONTEND LOCAL DEVELOPMENT
# =============================================================================
# Copy this to .env.local for local development
#
# ARCHITECTURE:
# - Frontend is a thin proxy to the backend
# - Frontend NEVER connects directly to Postgres or OpenAI
# - All database, embedding, and LLM operations happen on the backend
# =============================================================================

# =============================================================================
# BACKEND API (Required)
# =============================================================================
BACKEND_API_URL=http://localhost:8001

# =============================================================================
# INTERNAL API KEY (Required)
# =============================================================================
# Must match the INTERNAL_API_KEY on the backend
# Used for authenticating frontend→backend requests
INTERNAL_API_KEY=

# =============================================================================
# AUTHENTICATION (Optional)
# =============================================================================
# Password to access the frontend (leave empty for no password)
APP_PASSWORD=
# Secret for session cookies (generate with: openssl rand -hex 32)
APP_SESSION_SECRET=

# =============================================================================
# NOTE: The following are NOT needed on the frontend:
# - OPENAI_API_KEY (backend handles all OpenAI calls)
# - DATABASE_URL (backend handles all database operations)
# - EMBEDDING_* variables (backend handles embeddings)
# =============================================================================
