# =============================================================================
# DR. CHAFFEE AI - FRONTEND LOCAL DEVELOPMENT
# =============================================================================
# Copy this to .env.local for local development
# See .env.example for full documentation of all variables
# =============================================================================

# =============================================================================
# BACKEND API (Required)
# =============================================================================
# Local backend (if running backend locally)
BACKEND_API_URL=http://localhost:8001

# Or use production backend for testing:
# BACKEND_API_URL=https://drchaffee-backend.onrender.com

# =============================================================================
# DATABASE (Optional - only for answer caching)
# =============================================================================
# Note: Stats now come from the backend /stats endpoint via BACKEND_API_URL.
# DATABASE_URL is only needed if you want local answer caching in the frontend.
# In production, the backend handles all database access.
# Local PostgreSQL (optional):
# DATABASE_URL=postgresql://postgres:password@localhost:5432/askdrchaffee

# =============================================================================
# OPENAI API (Required for answer generation)
# =============================================================================
OPENAI_API_KEY=sk-proj-your-openai-key-here
SUMMARIZER_MODEL=gpt-4o-mini

# =============================================================================
# ANSWER SETTINGS
# =============================================================================
ANSWER_ENABLED=true
ANSWER_TOPK=100
ANSWER_TTL_HOURS=336
ANSWER_CACHE_TTL_HOURS=336
ANSWER_STYLE_DEFAULT=concise

# =============================================================================
# AUTHENTICATION (Optional)
# =============================================================================
# Leave empty to disable password protection
APP_PASSWORD=
