# Minimal requirements for Render CPU deployment
# Heavy ML packages (torch, pyannote, speechbrain) are excluded
# These are only needed for ingestion, which runs as a cron job

# Core dependencies
psycopg2-binary>=2.9.9
alembic>=1.13.0
sqlalchemy>=2.0.07
python-dotenv==1.0.0

# Web API dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Database and async
redis==5.0.1
asyncio-throttle==1.0.2
aiohttp==3.9.1

# YouTube API
google-api-python-client==2.108.0
google-auth-httplib2==0.1.1
google-auth-oauthlib==1.1.0

# Embeddings (lightweight)
sentence-transformers>=2.7.0
transformers>=4.41.0
openai>=1.0.0

# Utilities
tqdm==4.66.1
isodate==0.6.1
psutil>=5.9.0
