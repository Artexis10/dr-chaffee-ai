# --- Point to production DB on Hetzner ---
DATABASE_URL=postgresql://postgres:<prod_password>@<hetzner-db-host>:5432/postgres

# --- Embeddings: MATCH PROD MODEL, but allow GPU locally ---

# Do NOT set FORCE_CPU_ONLY here
# FORCE_CPU_ONLY is ONLY for Hetzner

EMBEDDINGS_PROVIDER=sentence-transformers
EMBEDDINGS_MODEL=BAAI/bge-small-en-v1.5
EMBEDDINGS_DIM=384

# Optional: if you want to be explicit, but not required
# EMBEDDING_PROFILE=speed

# Don't set EMBEDDINGS_DEVICE here; let the code auto-detect:
# - if torch.cuda.is_available() -> cuda
# - else -> cpu
