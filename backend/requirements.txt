# Core dependencies
psycopg2-binary>=2.9.9
alembic>=1.13.0
sqlalchemy>=2.0.07
python-dotenv==1.0.0
numpy>=1.24.3,<2.0.0  # Use >=1.24.3 for Python 3.12 compatibility (pre-built wheels)
tqdm==4.66.1

# Web API dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1
celery==5.3.4
redis==5.0.1

# YouTube transcript fetching
youtube-transcript-api==0.6.1
yt-dlp>=2023.11.16  # Latest for better reliability

# YouTube Data API
google-api-python-client==2.108.0
google-auth-httplib2==0.1.1
google-auth-oauthlib==1.1.0

# Text processing and embeddings
sentence-transformers>=2.7.0
transformers>=4.41.0

# Enhanced Audio transcription with latest Whisper models
# Removed WhisperX - using faster-whisper + asr_diarize_v4 directly
faster-whisper>=1.0.2  # Supports large-v3, distil-large-v3, large-v3-turbo
ctranslate2>=4.4.0  # CTranslate2 for faster-whisper (4.6.0+ for CUDA 13.0)
# Torch for ML models (Python 3.12 requires >=2.2.0)
# Note: CPU-only version is fine for production (bulk processing done locally with GPU)
torch>=2.2.0,<2.9.0
torchaudio>=2.2.0,<2.9.0
# System monitoring for VRAM tracking
psutil>=5.9.0

# Advanced ASR with speaker identification
pyannote.audio>=4.0.0  # v4 with new community pipeline and exclusive mode
speechbrain>=0.5.16  # For voice embeddings (speaker identification)
soundfile>=0.13.1  # Audio I/O (required by pyannote.audio 4.0.0)
webvtt-py>=0.5.1  # VTT subtitle parsing (maintained fork)

# Date/time parsing
isodate==0.6.1

# Async/concurrent processing
asyncio-throttle==1.0.2
apscheduler==3.10.4
aiohttp==3.12.14
aiohttp-socks==0.8.4

# Optional development tools
black==23.9.1
ruff==0.0.292
pre-commit==3.4.00
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
pytest-asyncio>=0.21.1
pytest-timeout>=2.1.0
freezegun>=1.2.2
hypothesis>=6.82.0

# Note: For Zoom API, we'll use direct requests instead of pyzoom
