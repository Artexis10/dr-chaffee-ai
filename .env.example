# =============================================================================
# DR. CHAFFEE AI - ROOT ENVIRONMENT VARIABLES (TOOLING/SCRIPTS ONLY)
# =============================================================================
# ⚠️  THIS FILE IS NOT USED BY THE FRONTEND OR BACKEND AT RUNTIME
#
# For app-specific configuration, use:
#   - Frontend: frontend/.env.local (copy from frontend/.env.example)
#   - Backend:  backend/.env (copy from backend/.env.example)
#
# This root .env is only used by:
#   - Docker Compose (docker-compose.dev.yml)
#   - Local helper scripts
#   - Development tooling
# =============================================================================

# =============================================================================
# DOCKER COMPOSE CONFIGURATION
# =============================================================================
# These are used by docker-compose.dev.yml for local development with Docker

# PostgreSQL container settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=askdrchaffee

# Database URL for Docker containers (uses container hostname)
DATABASE_URL=postgresql://postgres:password@db:5432/askdrchaffee

# =============================================================================
# LOCAL DEVELOPMENT SCRIPTS
# =============================================================================
# These may be used by helper scripts in the scripts/ directory

# YouTube channel for ingestion scripts
YOUTUBE_CHANNEL_URL=https://www.youtube.com/@anthonychaffeemd

# =============================================================================
# NOTE: All other configuration has been moved to app-specific env files
# =============================================================================
# See:
#   - frontend/.env.example - Frontend environment variables
#   - backend/.env.example  - Backend environment variables (comprehensive)
# =============================================================================
